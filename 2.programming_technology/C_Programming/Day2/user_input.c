#include<stdio.h>

//Here & is address of memory
//&a -> address of memory
//a -> value of the memory
int main(){
	int a;
	char x;
	scanf("%d",&a); //Taking user input
	scanf("%c",&x);
	printf("We got -> %d",a); // printing on the terminal
	return 0;
}
